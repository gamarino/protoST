# protoST — Design

The canonical design specification lives at:

**[docs/superpowers/specs/2026-05-19-protost-design.md](docs/superpowers/specs/2026-05-19-protost-design.md)**

This file is a pointer so that the project root follows the same convention as `protoCore/`, `protoJS/`, and `protoPython/`. New design iterations are added as additional dated documents under `docs/superpowers/specs/` rather than overwriting this one.
