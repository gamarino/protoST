"EXPECT: 3.14"
"§2.5 + D11: a float literal lexes, parses and prints as its decimal value.
`printString` of a Float is now bound on the numeric tower and renders the
value, not the class name."
3.14 printString.
