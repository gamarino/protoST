"EXPECT: -1"
"§2.4 + D1: a `-` immediately followed by a digit, in operand/primary
position (no intervening operand), lexes as the sign of a negative numeric
literal. Writing the literal -1 directly yields -1."
-1.
