"EXPECT: true"
"§7 / §9.2: the language reference's own examples send `isEven` to an integer
(e.g. `aCollection do: [ :x | x isEven ifTrue: [ ^ x ] ]` in §7, and
`select: [ :e | e isEven ]` in §9.2). `isEven` is bound on SmallInteger."
4 isEven.
